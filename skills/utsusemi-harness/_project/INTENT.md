# {{PROJECT_NAME}} — Intent

This file states what the project is, why it exists, and what it deliberately is not. Everywhere else in the spec layer the code has the last word: when `README.md` or `SPEC/` disagrees with it, the code wins and the document is updated. Here that reverses — when this file disagrees with the code, the code is what is suspect.

## What

> _One or two sentences: what this is._

## Why

> _One or two sentences: whose problem this removes. This is what an agent falls back on when a task does not say enough._

## Non-goals

> _Optional. Explicit exclusions, worth writing only where the project has a plausible adjacent thing it could drift into becoming — "no plugin system", "not a general-purpose X". Delete this section if nothing here is real; an empty heading invites filler._
